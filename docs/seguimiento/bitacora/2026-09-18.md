# 2026-09-18: agente en vivo con voz bidireccional

Zona horaria: America/Bogota.

## Objetivo

Que el agente en vivo, mientras el usuario comparte pantalla, tambien escuche
y hable por voz real (no solo texto y capturas puntuales), decida por su
cuenta cuando preguntar algo, y que esa conversacion alimente el mismo
informe que hoy produce el analisis de un video subido.

## Cambios realizados

- Corregido el shutdown de los workers locales (`cognitive-consolidate`,
  `cognitive-analyze_recording`, `cognitive-index_recording`): el
  `KeyboardInterrupt` podia llegar durante los imports pesados (cadena hacia
  `azure.core`), antes del `try/except` que ya protegia el bucle principal,
  imprimiendo un traceback por proceso al cerrar con Ctrl+C
  (`src/cognitive_os/workers/runtime.py`).
- Nuevo `WS /api/v1/learning-sessions/{session_id}/agent/live-voice`
  (`src/cognitive_os/api/v1/endpoints/agent.py`), distinto del `/agent/live`
  existente (que queda sin cambios de protocolo): streaming continuo de audio
  binario, capturas de pantalla periodicas, y preguntas proactivas del modelo
  via tool call (`ask_clarifying_question`), mediado siempre por el backend
  hacia OpenAI Realtime API (nunca se expone la API key ni un token efimero
  al navegador).
- `src/cognitive_os/infrastructure/ai/openai_realtime.py` (nuevo): adaptador
  a la Realtime API de OpenAI.
- `src/cognitive_os/application/live_agent.py`: factorizado el gating de
  preguntas (`gate_and_persist_clarification`) para que lo compartan el canal
  de turnos y el de voz; nuevas funciones de ciclo de vida de la sesion de voz
  (`start_voice_session`, `end_voice_session`, `append_voice_transcript`,
  `check_live_status`, `resolve_clarification`, `create_voice_clarification`).
- Migracion `010_realtime_voice.sql`: tabla `realtime_voice_sessions` (una
  sesion de voz activa por `learning_session`, forzado con indice unico
  parcial) y nuevo `event_type` `realtime_voice_transcript` en `session_events`.
- `src/cognitive_os/workers/visual.py`: si la sesion tuvo una llamada de voz
  en vivo, el worker `analyze_recording` usa esa transcripcion directamente y
  no vuelve a transcribir el audio del video con Whisper
  (`sampling.audio_exclusion_reason = "live_voice_transcript_available"`).
- Settings nuevos en `src/cognitive_os/core/config.py`: modelo/URL de
  Realtime, cadencia de capturas, TTL de sesion, intervalo de reautenticacion,
  concurrencia maxima y tope de tamano de audio.
- Dependencia nueva: `websockets` (declarada explicitamente en
  `pyproject.toml`; ya llegaba transitiva via `uvicorn[standard]`).
- Documentacion: nueva ficha
  [`endpoints/63-agent-live-voice.md`](../endpoints/63-agent-live-voice.md);
  `endpoints/62-agent-live.md` reducido para enlazar a la nueva en vez de
  duplicar contrato; `guias/06-aprendizaje-visual.md` y
  `base-de-datos/04-video-y-vectores.md` actualizados con el flujo y las
  tablas nuevas; `guias/07-mejoras-frontend.md` con una seccion 7 completa de
  lo que falta implementar en el frontend (conexion, microfono/TTS, capturas,
  preguntas proactivas, prueba de aceptacion).

## Verificacion

- 80 pruebas backend contra un cluster PostgreSQL real, aislado y desechable
  (`scripts/test_postgres.ps1`), incluida la migracion 010 aplicada limpio.
- Pruebas nuevas: auth/ready, rechazo de una segunda sesion de voz concurrente
  (409), y reenvio de audio/captura + creacion de clarificacion via tool call
  en `/agent/live-voice` (`tests/test_live_agent.py`); no-doble-transcripcion
  en el worker cuando existe transcripcion de voz en vivo
  (`tests/test_recordings.py`).
- `/agent/live` verificado sin cambios de comportamiento (mismas pruebas
  previas siguen pasando).
- No se hizo ninguna llamada real de pago a OpenAI Realtime ni se probo con
  audio real de un navegador.

## Pendientes

- Verificar el formato exacto de eventos de la Realtime API de OpenAI (nombres,
  forma de `session.update`, function calling) contra la documentacion vigente
  antes de una prueba con cuenta real; se implemento segun lo conocido al
  escribir el codigo y esa superficie evoluciona.
- Todo el lado de frontend: captura de microfono, reproduccion de audio TTS,
  UI de preguntas proactivas y manejo de `session.ending`. Ver
  [guias/07-mejoras-frontend.md](../guias/07-mejoras-frontend.md), seccion 7.
- Probar con credenciales reales de OpenAI Realtime en un entorno con acceso
  habilitado.

## Referencias

- Migracion: `migrations/010_realtime_voice.sql`.
- Fichas: [`endpoints/63-agent-live-voice.md`](../endpoints/63-agent-live-voice.md),
  [`endpoints/62-agent-live.md`](../endpoints/62-agent-live.md).
- Guias: [`guias/06-aprendizaje-visual.md`](../guias/06-aprendizaje-visual.md),
  [`guias/07-mejoras-frontend.md`](../guias/07-mejoras-frontend.md).
- Base de datos: [`base-de-datos/04-video-y-vectores.md`](../base-de-datos/04-video-y-vectores.md).

---

# 2026-09-18: consolidacion inteligente de la base de conocimiento

## Objetivo

El cliente puede volver a ensenar o corregir el mismo procedimiento en otra
sesion/grabacion. Antes, `index_recording` solo acumulaba vectores por
grabacion: dos versiones de un mismo hecho (una vieja y una corregida) podian
convivir como resultados activos de `POST /recordings/search`. El pedido era
separar responsabilidades entre modelos (uno que conversa, otro que transcribe,
otro que arma documentos, y uno que organiza la base de conocimiento) y que
esta ultima decida con inteligencia cuando algo nuevo reemplaza algo existente.

## Cambios realizados

- Migracion `011_knowledge_consolidation.sql`: agrega `status` (`active`/
  `superseded`) y `superseded_by` a `cognitive.recording_vectors`. Aditiva;
  las filas existentes quedan `active`.
- `src/cognitive_os/infrastructure/ai/openai_curator.py` (nuevo): modelo
  curador dedicado (`OPENAI_CURATOR_MODEL`), separado del modelo de
  conversacion en vivo, del de transcripcion y del de generacion de informes.
  Solo recibe pares cortos de fragmentos ya preseleccionados por similitud de
  embedding; nunca ve la sesion completa ni conversa con el usuario.
- `src/cognitive_os/application/recording_knowledge.py`: `find_supersede_candidates`
  (filtro por similitud de embedding, mismo `procedure_id`, otra grabacion) y
  `search_vectors` ahora filtra `status='active'`.
- `src/cognitive_os/workers/recording_index.py`: `consolidate_knowledge` inserta
  primero los fragmentos nuevos (el `superseded_by` exige que ya existan por la
  llave foranea), busca candidatos por cada fragmento nuevo y, solo si hay
  candidatos, llama al curador; aplica sus decisiones con `UPDATE ... status=
  'superseded'`. Sin `procedure_id` o sin candidatos, el curador no se llama.
- `src/cognitive_os/workers/__main__.py` y `runtime.py`: instancian y cierran
  el curador junto al resto de proveedores para el `kind=index_recording`.
- Settings nuevos: `OPENAI_CURATOR_MODEL` (por defecto `gpt-5.6-luna`) y
  `COGNITIVE_KNOWLEDGE_SUPERSEDE_SIMILARITY` (0.75 por defecto).

## Verificacion

- 82 pruebas backend contra un cluster PostgreSQL real, aislado y desechable
  (`scripts/test_postgres.ps1`), incluida la migracion 011 aplicada limpio.
- Prueba nueva `test_knowledge_consolidation_supersedes_outdated_fragment`
  (`tests/test_recording_knowledge.py`): dos grabaciones del mismo procedimiento,
  un curador simulado (`AlwaysSupersede`) marca los fragmentos de la primera
  como `superseded` y `POST /recordings/search` solo devuelve los de la segunda.
- No se hizo ninguna llamada real de pago al modelo curador; el proveedor real
  (`OpenAIKnowledgeCurator`) no se ejecuto contra la API de OpenAI.

## Pendientes

- Aplicar la migracion 011 sobre la base `cognitive` local (sigue igual que las
  anteriores: manual, desde Query Tool, en orden).
- Medir con datos reales si 0.75 de similitud es el umbral correcto para
  preseleccionar candidatos sin perder actualizaciones legitimas ni generar
  llamadas de mas al curador.
- El curador compara fragmentos de grabaciones distintas del mismo
  `procedure_id`; no cubre todavia una consolidacion entre procedimientos
  distintos ni entre el borrador de un procedimiento y su version publicada.

## Referencias

- Migracion: `migrations/011_knowledge_consolidation.sql`.
- Guia: [`guias/06-aprendizaje-visual.md`](../guias/06-aprendizaje-visual.md),
  seccion "Consolidacion de conocimiento entre grabaciones".
